version: "3.9"

services:
  heplify-ng:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: sipcapture/heplify-ng:latest
    container_name: heplify-ng
    restart: unless-stopped
    network_mode: host
    cap_add:
      - NET_ADMIN
      - NET_RAW
    volumes:
      - ./config/heplify.json:/etc/heplify-ng/heplify.json:ro
      - ./config/script.lua:/etc/heplify-ng/script.lua:ro
    environment:
      - HEPLIFY_CONFIG=/etc/heplify-ng/heplify.json
    command:
      - -config
      - /etc/heplify-ng/heplify.json
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
